namespace CpuWidget;

/// <summary>
/// Displayed in the bottom-right corner of the widget.
/// Bump the last digits on every change that ships — v.1.051, v.1.052, and so on.
/// Keep <c>Version</c> in CpuWidget.csproj in step so the exe's file properties match.
/// </summary>
public static class AppVersion
{
    public const string Display = "v.1.051";
}
